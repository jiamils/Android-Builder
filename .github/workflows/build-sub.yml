name: Generate SingBox Config
on:
  workflow_dispatch:
  schedule:
    - cron: '0 */8 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install SubBridge
        run: npm install -g @0xsnx/subbridge

      # ==========新增这一段，把npm全局目录加入PATH==========
      - name: Export npm global path
        run: |
          echo "PATH=$(npm config get prefix)/bin:$PATH" >> $GITHUB_ENV

      - name: Run SubBridge Convert
        env:
          SUB_LINK: ${{ secrets.SUB_URL }}
        run: |
          subbridge build \
            -i "${SUB_LINK}" \
            -t ./template.json \
            -o ./docs/singbox.json

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
